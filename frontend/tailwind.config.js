/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: ["class"],
    content: [
      "./index.html",
      "./src/**/*.{ts,tsx}"
    ],
    theme: {
      extend: {
        // TODO: same or simpler theme
      }
    },
    plugins: [
        // TODO: tailwindcss-animate
    ]
  }
  